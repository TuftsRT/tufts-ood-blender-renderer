batch_connect:
  template: "basic"

script:
  job_name: "Blender_renderer"
  native:
    - "--gres=gpu:1"
    - "--constraint=l40s"
    - "--partition=gpu"
    - "--mem=<%= num_memory %>G"
    - "--qos=expanded4x"
    - "--ntasks=4"
#    - "--ntasks-per-node=4"
#    - "--nodes=2"
    - "--cpus-per-task=8"
    - "--gpus-per-task=1"
    - "--time=04:00:00"
#    - "--output=slurm-%x-%j.out"
    - "--error=slurm-%x-%j.err"