batch_connect:
  template: "basic"

script:
  job_name: "Blender_renderer"
  native:
    - "--gres=gpu:1"
    - "--constraint=l40s"
    - "--partition=gpu"
    - "--cpus-per-task=<%= num_cores %>"
    - "--mem=<%= num_memory %>G"
    - "--qos=expanded"
