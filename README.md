# tufts-ood-blender-renderer
Open OnDemand app to render animations using Blender on a HPC cluster using GPUs.  The form accepts the path to a Blender file, an output director, and the HPC resources to use.  Once started, the render job progress can be monitored via the app card.



# Todo
* Better GPU selection, ideally with GPU auto detection
* Support for choosing the frames to render vs all
* Better final video rendering stage
